export type StyleReference = {
    artist: string;
    artwork: {
      url: string;
    };
    id?: string;
  };

export type ComicStyle = {
    artist: string;
    artwork: {
      url: string;
    };
    id?: string;
  };